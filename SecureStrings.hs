
-- Carnap-Server/SecureStrings.hs
-- shifted around rev 254
-- ~ rev 465, material is deleted for Google Pages

module SecureStrings where

import qualified Data.Text as T
import           Data.Text ( Text )


googleApiKey :: Text
googleApiKey = error "no googleApiKey!!!"

googleSecret :: Text
googleSecret = error "no googleSecret!!!"


